# linux.do 访问相关 —— 全部走代理
#
# 这份表解决一件事：让 Dexo（iOS 的 Discourse 客户端）能访问 linux.do。
# 论坛本身和它在客户端里配的三个 DoH 端点都在这里，同一个策略。
#
# 位置要求（overlay 的 verify() 会检查，顺序错了是静默失效）：
#   1. 必须早于 WSure00/ProxyResource 那份个人直连表 —— 那里有
#      DOMAIN-SUFFIX,linux.do,China，会把论坛按 DIRECT 处理，直连必死。
#   2. 必须早于 BlockHttpDNS.list —— 那份表每天现拉 blackmatrix7 上游，
#      一旦哪天收录这类社区 DoH，就会落进默认 REJECT 的 🧱 DNS 防泄露。
#
# ── linux.do 为什么只能走代理 ────────────────────────────────────────
# 被墙，且墙认的是 SNI 不是 IP。2026-08-02 实测同一个 Cloudflare IP
# 172.66.166.61，只换 SNI：
#     SNI=linux.do     -> 发完 TLS ClientHello 即「连接被对方重置」
#     SNI=example.com  -> 握手正常完成
# 所以"用 DoH 查到干净 IP 再把这些 IP 设成直连"这条路根本不通，只能走代理。
#
# 国内 DNS 对它污染也很彻底，三个解析源各给一个不同的假 IP：
#     alidns DoH    -> 64.13.192.76    (直连超时)
#     119.29.29.29  -> 157.240.0.18    (Facebook 的段)
#     223.5.5.5     -> 45.77.186.255   (Vultr 的段)
# 真实 IP 是 172.66.166.61 / 104.20.16.234，Cloudflare anycast 会漂，
# 所以一律按域名写规则，不按 IP。
#
# 根治办法是去 ProxyResource 删掉那行 linux.do；这里做覆盖是为了不跨仓库
# 改动也能生效。
#
# ── 三个 DoH 端点为什么也走代理 ──────────────────────────────────────
# 它们国内直连本来就通（实测 0.2~1.8s 拿到未污染应答），设成直连会稍快一点。
# 但既然合成一份表就只能一个策略，而 linux.do 必须走代理，就跟着走代理。
#
# 代价可以接受，而且实测确认了两点：
#   - 两个活端点都不广告 alt-svc，客户端不会升级到 h3，所以
#     block-quic = all-proxy 对它们没有影响。
#   - 两个端点走 HTTP/2 都是 200，境外可达，经代理不会失败。
# 反过来还有个好处：哪天这些端点自己被 SNI 封了，走代理仍然能用。
#
# ── 各端点实测状态（2026-08-02，本机在国内）─────────────────────────
# edge.47258.xyz        HTTP/2 200，0.22s，稳定。只认 RFC8484 wireformat，
#                       发 dns-json 一律返回 400。
# wsu.ddd.oaifree.com   HTTP/2 200，0.3~1.8s。wireformat 和 dns-json 都支持。
# gameapi.47258.xyz     三个 IP 全不通。TLS 握手能完整走完（证书有效），之后
#                       服务端不协商 ALPN、0 字节超时，源站疑似已死。留在这里
#                       只是为了它哪天恢复；客户端里建议先摘掉，否则 DoH 轮询
#                       会卡在它的超时上。
#
# 注意：走代理的流量由落地节点重新解析，客户端自己用 DoH 查到的 IP 在那一步
# 会被丢掉。所以客户端内配 DoH 对 linux.do 这类走代理的域名基本是装饰性的，
# 真正让它能通的是代理本身。

DOMAIN-SUFFIX,linux.do
DOMAIN,edge.47258.xyz
DOMAIN,wsu.ddd.oaifree.com
DOMAIN,gameapi.47258.xyz
