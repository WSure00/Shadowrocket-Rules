# linux.do 访问相关 —— 全部直连
#
# 这份表解决一件事：让 Dexo（iOS 的 Discourse 客户端）能访问 linux.do。
# 论坛本身和它在客户端里配的三个 DoH 端点都在这里，统一走 DIRECT。
#
# ── 光有这份表不够，必须配合 [Host] 段 ───────────────────────────────
# 走 DIRECT 意味着由 Shadowrocket 用 [General] 里的 dns-server 自己解析，
# 而那几个国内 DNS 对 linux.do 污染得很彻底（2026-08-02 实测）：
#     alidns DoH    -> 108.160.167.174  (Dropbox 段)
#     119.29.29.29  -> 31.13.75.12      (Facebook 段)
#     223.5.5.5     -> 108.160.167.174
#     干净 DoH      -> 172.66.166.61 / 104.20.16.234  (真实 Cloudflare)
# 上面那几个污染 IP 全部连不通。所以只把规则设成 DIRECT、不改解析的话，
# 会出现"规则确实命中了 DIRECT，但页面还是打不开"——解析拿到的是假 IP。
#
# 因此 overlay 会同时往 [Host] 段写：
#     linux.do   = server:https://wsu.ddd.oaifree.com/query-dns
#     *.linux.do = server:https://wsu.ddd.oaifree.com/query-dns
# 让 Shadowrocket 自己也用干净 DoH 解析这个域名。verify() 会检查这几行在不在。
#
# 真实 IP 是 Cloudflare anycast，会漂，所以规则按域名写、解析交给 DoH，
# 不往 [Host] 里钉死 IP。
#
# ── 位置要求（verify() 会检查，顺序错了是静默失效）──────────────────
# 1. 早于 WSure00/ProxyResource 那份个人直连表。那里本来就有
#    DOMAIN-SUFFIX,linux.do,China，策略同样是 DIRECT，所以这条不影响结果；
#    固定顺序只是为了不因上游改动而漂。
# 2. 早于 BlockHttpDNS.list。那份表每天现拉 blackmatrix7 上游，一旦哪天
#    收录了表里这类社区 DoH，就会落进默认 REJECT 的 🧱 DNS 防泄露。
#
# ── 各 DoH 端点实测状态（2026-08-02，直连）──────────────────────────
# edge.47258.xyz        HTTP/2 200，0.22s，稳定。只认 RFC8484 wireformat，
#                       发 dns-json 一律返回 400。
# wsu.ddd.oaifree.com   HTTP/2 200，0.3~1.8s。wireformat 和 dns-json 都支持，
#                       所以 [Host] 里用的是它。
# gameapi.47258.xyz     三个 IP 全不通。TLS 握手能完整走完（证书有效），之后
#                       服务端不协商 ALPN、0 字节超时，源站疑似已死。留在这里
#                       只是为了它哪天恢复；客户端里建议先摘掉，否则 DoH 轮询
#                       会卡在它的超时上。

DOMAIN-SUFFIX,linux.do
DOMAIN,edge.47258.xyz
DOMAIN,wsu.ddd.oaifree.com
DOMAIN,gameapi.47258.xyz
